# Configurações do Django
# Configurações Gerais
DEBUG=1

SECRET_KEY='django-insecure-vlvldu*s!z!*e39=r+gnk1*7353cb$e5q&vbw)r3vro$vh1@tz'

# Chave secreta do Django (use uma chave forte em produção)

# Configurações de Hosts
ALLOWED_HOSTS=127.0.0.1, localhost

# Configurações do Banco de Dados
DB_ENGINE=django.db.backends.postgresql

POSTGRES_DB="RECIPE"
POSTGRES_USER="castilho_user"
POSTGRES_PASSWORD=castilho_pas
POSTGRES_HOST=psql
POSTGRES_PORT=5432