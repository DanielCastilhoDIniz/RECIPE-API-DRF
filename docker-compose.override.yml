
services:
  app:
    env_file:
      - ./dotenv_files/.env
    build:
      args:
        - DEV=true
    volumes:
      - ./app:/app
      - ./data/web/static:/data/web/static/
      - ./data/web/media:/data/web/media/
